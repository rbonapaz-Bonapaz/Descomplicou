# Sessões/config locais do Claude Code — nunca devem ir pro repositório (podem conter tokens,
# transcrições de conversa e dados de sessão).
.claude/

# Material de referência local (PDFs de pedidos reais, planilhas, docs pessoais, backups) — não é
# código do projeto e não deve ser publicado.
Arquivos para consulta/

# SO / editor
.DS_Store
Thumbs.db

# Cloud Functions — dependências instaladas (reconstrói com `cd functions && npm install`) e logs
# locais do emulador/CLI. package-lock.json FICA versionado (garante o build reproduzível no
# deploy do Firebase).
functions/node_modules/
firebase-debug.log
.firebase/
